# Postgres connection used by the Debezium connector setup script.
# The connector runs inside Docker → use the service name "postgres"
# and the internal port 5432.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<change_me>
POSTGRES_DB=banking
